---
title: "РџСЂРѕРіРЅРѕР·С‹ РїСЂРёС‚РѕРєР° Рє РѕР·РµСЂСѓ Р‘Р°Р№РєР°Р»"
author: "Р’СЃРµРІРѕР»РѕРґ РњРѕСЂРµР№РґРѕ"
date: "23 РѕРєС‚СЏР±СЂСЏ 2020 Рі."
output:
  pdf_document: default
---

**Байкал** _(бур. Байгал далай)_ — [озеро](https://ru.wikipedia.org/wiki/%D0%91%D0%B0%D0%B9%D0%BA%D0%B0%D0%BB) тектонического происхождения в южной части Восточной Сибири, самое глубокое озеро на планете, крупнейший природный резервуар пресной воды и самое большое по площади пресноводное озеро на континенте.

Озеро и прибрежные территории отличаются уникальным разнообразием флоры и фауны, бо́льшая часть видов животных эндемична.

# Прогнозы притока

* прогнозы месячного разрешения
* прогнозы весеннего половодья
* прогнозы летне-осенних паводков
  + и максимальных уровней воды
  
```{r}
load(file = 'd:/YandexDisk/ИВПРАН/R forecasts/байкал/prog_df.RData')
summary(prog_df)
```

Приток к Байкалу выглядит так:

```{r, warning=FALSE, fig.width=10, fig.width=10, message=FALSE}
library(ggplot2)
ggplot(prog_df, aes(x=year, y=obs, col=month)) + geom_line() + geom_point() + theme_light(base_size = 15)
```

# Оценка ошибок прогноза

Расчет систематической ошибки прогноза производится по формуле

$ME = \frac 1n  \sum q_{forecast} - q_{observed}$, 

где $q_{forecast}$ - прогнозный расход воды.

Среднее значение притока воды в озеро Байкал составляет `r round(mean(prog_df$obs, na.rm=T), 2)` м^3^/c.
